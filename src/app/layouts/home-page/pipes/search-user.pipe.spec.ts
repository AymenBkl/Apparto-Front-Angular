import { SearchFaqPipe } from './search-user.pipe';

describe('SearchFaqPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFaqPipe();
    expect(pipe).toBeTruthy();
  });
});
